'''
import sys
import figlet
argv = sys.argv

f = pyfiglet.Figlet()
text = input("Input: ")
font_styles = f.getFonts()
font_style = random.choice(font_styles)

if len == 1 ==> use random font and get the result
elif len == 3 and argv[1] == "-f" or "--font" and argv[2] in available_fonts:
    ==> chenge the default font
    get the result
else ==> print error by using sys.exit

print(result)
'''
